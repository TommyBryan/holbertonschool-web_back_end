// Define and export a function named setFromArray
export default function setFromArray(array) {
  // Create a new Set from the input array
  // A Set automatically removes duplicate values
  return new Set(array);
}
